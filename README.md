# PDF Knowledge Base Bot

This project is a POC AI bot that reads PDF files, extracts content, performs semantic search, and answers user questions using OpenAI GPT models.

## Installation

```bash
pip install -r requirements.txt
